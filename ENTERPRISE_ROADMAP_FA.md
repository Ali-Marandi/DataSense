# نقشه‌راه تجاری و سازمانی DataSense

**وضعیت:** مصوب برای پیاده‌سازی نسخهٔ بعدی  
**محصول:** DataSense for Windows  
**مالک محصول:** Ali Marandi  
**تهیه‌کننده:** Manus AI

## چشم‌انداز محصول

DataSense باید از یک ابزار تحلیل تک‌کاربره به یک **استودیوی تحلیل دادهٔ قابل‌اعتماد** تبدیل شود: محصولی محلی-اول برای ویندوز که تحلیل‌گر بتواند داده را وارد کند، کیفیت و حساسیت آن را بسنجد، قواعد قابل‌تکرار اعمال کند، تحلیل و مدل بسازد و خروجی قابل‌ممیزی ارائه دهد. این جایگاه، فاصلهٔ میان سرعت ابزارهای self-service و الزامات کنترل، شفافیت و تکرارپذیری سازمانی را پوشش می‌دهد.

پژوهش بازار نشان می‌دهد که رقابت در سطح سازمانی تنها با نمودار و مدل‌سازی انجام نمی‌شود. Power BI بر مدل معنایی قابل‌اعتماد، اشتراک امن و اقدام‌پذیرسازی بینش تأکید می‌کند؛ Tableau نیز اتصال به داده، لایهٔ معنایی امن، API/connector و حاکمیت را معیار انتخاب محصول می‌داند. [1] [2] ابزارهای Alteryx، KNIME و Dataiku نیز lineage، versioning، کنترل دسترسی، اجرای قابل‌تکرار، ثبت رویداد، استقرار منعطف و حاکمیت چرخهٔ عمر AI را به‌عنوان مؤلفه‌های اصلی عرضه می‌کنند. [3] [4] [5]

> **اصل طراحی:** پاسخ تولیدشده با AI یا هر نتیجهٔ تحلیلی نباید تنها «قانع‌کننده» باشد؛ باید به منبع داده، منطق محاسبه، قاعدهٔ کیفیت، زمان اجرا و وضعیت اعتبارسنجی قابل ردیابی باشد.

| افق | هدف تجاری | نتیجهٔ ملموس برای کاربر |
|---|---|---|
| نسخهٔ 2.2 — Trust Center | اعتمادپذیری داده پیش از تحلیل | طبقه‌بندی PII، قرارداد کیفیت، امتیاز و گزارش ممیزی محلی |
| نسخهٔ 2.3 — Workflow Studio | تکرارپذیری و اتوماسیون | جریان‌های تبدیلی نسخه‌دار، اجرای زمان‌بندی‌شده و ثبت lineage |
| نسخهٔ 2.4 — Semantic & Collaboration | مقیاس تیمی | فرهنگ لغت داده، معیارهای مشترک، workspace، review و نقش‌ها |
| نسخهٔ 3.0 — Governed AI | AI سازمانی و کنترل‌پذیر | gateway مدل، بودجه/سقف مصرف، ارزیابی خروجی، human approval و audit trail |

## شکاف‌های مشاهده‌شده و اولویت توسعه

نسخهٔ فعلی به‌طور قابل‌توجهی از قابلیت‌های پایه عبور کرده است: آماده‌سازی، آمار، مدل‌سازی، AutoML، تحلیل سری زمانی، SQL محلی، داشبورد و گزارش را ارائه می‌کند. آزمون پایه نیز در محیط توسعه با موفقیت اجرا شده است. با این حال، کیفیت داده در قالب **قرارداد اجرایی**، کشف دادهٔ حساس، گزارش ممیزی مستقل، نگهداری rule set همراه فایل پروژه، و گزارش‌پذیری وضعیت اعتماد داده هنوز به‌صورت یکپارچه وجود ندارد. این موارد، مانع اصلی استفادهٔ سازمانی از خروجی‌های تحلیلی خواهند بود؛ زیرا کیفیت، امنیت و یک منبع حقیقت از چالش‌های مرکزی تحلیل سازمانی‌اند. [2]

| اولویت | قابلیت پیشنهادی | ارزش تجاری | وضعیت این نسخه |
|---|---|---|---|
| P0 | Trust Center و Data Contract | جلوگیری از تحلیل دادهٔ نامعتبر و ایجاد evidence قابل ممیزی | **پیاده‌سازی می‌شود** |
| P0 | کشف PII و برچسب حساسیت | کاهش ریسک اشتراک/خروجی‌گرفتن از دادهٔ حساس | **پیاده‌سازی می‌شود** |
| P0 | گزارش JSON قابل حمل از کنترل‌ها | قابلیت ثبت در ticket، CI یا پروندهٔ کنترل داخلی | **پیاده‌سازی می‌شود** |
| P1 | Lineage گرافی و workflow canvas | تکرارپذیری، انتقال دانش و استانداردسازی | طراحی شده، نسخهٔ بعد |
| P1 | Data catalog و semantic metrics | کاهش ابهام در KPI و آماده‌سازی برای AI قابل‌اعتماد | طراحی شده، نسخهٔ بعد |
| P1 | اتصال‌های SSO/RBAC و vault | کنترل دسترسی و مدیریت اسرار در استقرار تیمی | طراحی شده، نسخهٔ بعد |
| P2 | AI gateway و evaluation harness | کنترل هزینه، provider switching و ارزیابی پاسخ AI | طراحی شده، نسخهٔ بعد |
| P2 | Collaboration hub و approval workflow | بازبینی، امضا و انتشار کنترل‌شدهٔ assetها | طراحی شده، نسخهٔ بعد |

## طراحی نسخهٔ 2.2: Trust Center

### معماری

هستهٔ جدید `core/governance.py` مستقل از رابط کاربری است و چهار مسئولیت روشن دارد: تشخیص برچسب‌های حساسیت، تعریف و اجرای قواعد کیفیت، تولید گزارش قابل‌انتقال و تبدیل نتایج به جدول‌های قابل‌نمایش. قواعد به‌صورت داده‌محور و قابل‌سریال‌سازی طراحی می‌شوند تا در فایل `.dsproj` ذخیره شده و بدون وابستگی به UI در آینده از CLI، زمان‌بند یا pipeline نیز اجرا شوند.

| جزء | مسئولیت | قرارداد خروجی |
|---|---|---|
| `SensitiveDataScanner` | تشخیص الگوهای ایمیل، تلفن، کارت، IP و نشانه‌های ستونی PII | `DataClassification` برای هر ستون |
| `DataQualityRule` | تعریف ruleهای not-null، unique، range، allowed-values و regex | JSON پایدار و قابل‌ویرایش |
| `DataContract` | نگهداری مجموعه rule و اجرای قطعی آن روی DataFrame | `QualityReport` با امتیاز و جزئیات تخلف |
| `QualityReport` | تفکیک pass/fail/error، محاسبه امتیاز و خروجی audit | جدول و JSON قابل حمل |
| `DataManager` | مالک contract فعال و snapshot آخرین ارزیابی | حاکمیت داده بخشی از وضعیت پروژه |

امتیاز کیفیت جایگزین قضاوت انسانی نیست. امتیاز صرفاً نسبت قواعد موفق به قواعد ارزیابی‌شده را با وزن قواعد نشان می‌دهد؛ قرارداد خالی نیز به‌جای امتیاز کامل، وضعیت **Not configured** می‌گیرد. این تصمیم از نمایش اعتماد کاذب جلوگیری می‌کند.

### تجربهٔ کاربری

برگهٔ جدید **Trust Center** به‌صورت یک workspace مستقل در پنجرهٔ اصلی اضافه می‌شود. کاربر ابتدا با یک دکمه PII scan را اجرا می‌کند، سپس با «Recommended rules» قرارداد اولیه می‌سازد، قواعد را به‌صورت صریح بازبینی می‌کند و با Run checks نتیجه را می‌بیند. موارد fail در جدول قابل‌فیلتر نمایش داده می‌شوند و گزارش JSON برای الحاق به مستندات کنترل داخلی قابل ذخیره است.

رعایت review انسانی در این طراحی عمدی است. مستندات Power BI هشدار می‌دهد که خروجی‌های AI ممکن است نادرست باشند و لازم است طراحی مدل و خروجی‌ها اعتبارسنجی شوند. [6] بنابراین پیشنهادهای DataSense در این نسخه deterministic و قابل‌بررسی هستند و هیچ قاعده‌ای بدون اقدام آگاهانهٔ کاربر داده را تغییر نمی‌دهد.

## برنامهٔ تجاری پیشنهادی

نسخهٔ پایه باید امکان تحلیل محلی و Trust Center را بدون ارسال داده به سرویس بیرونی نگه دارد. نسخهٔ حرفه‌ای می‌تواند شامل connector pack، workflow scheduler، templateهای صنعتی و dashboard export پیشرفته باشد. نسخهٔ سازمانی باید با استقرار on-premise یا private cloud، RBAC/SSO، catalog مشترک، audit retention و AI gateway عرضه شود. این مسیر با نیاز بازار به استقرار SaaS، on-premise یا hybrid و حاکمیت در تمام این مدل‌ها هم‌سو است. [3] [4]

| بسته | مخاطب | تمایز |
|---|---|---|
| Community / Desktop | تحلیل‌گر فردی | تحلیل محلی، قرارداد کیفیت، گزارش ممیزی و خروجی استاندارد |
| Professional | تیم تحلیل | workflow، connector pack، template و زمان‌بندی محلی |
| Enterprise | سازمان‌های حساس به داده | SSO/RBAC، vault، catalog، lineage، approval، audit و AI gateway |

## معیار پذیرش نسخهٔ 2.2

این نسخه تنها زمانی آمادهٔ انتشار تلقی می‌شود که قوانین روی داده‌های عددی و متنی قابل‌پیش‌بینی اجرا شوند، تشخیص PII دادهٔ اصلی را تغییر ندهد، قرارداد و نتیجه در `.dsproj` بازیابی شوند، JSON خروجی معتبر باشد، و تمام آزمون‌های موجود به‌علاوهٔ آزمون‌های جدید هستهٔ حاکمیت پاس شوند. نسخهٔ نهایی باید با PyInstaller روی Windows x64 ساخته و توسط CI منتشر شود.

## منابع

[1]: https://www.microsoft.com/en-us/power-platform/products/power-bi "Microsoft Power BI"
[2]: https://www.tableau.com/analytics/what-is-enterprise-analytics "Tableau — Enterprise Analytics"
[3]: https://www.alteryx.com/ "Alteryx One"
[4]: https://www.knime.com/knime-for-enterprise "KNIME for Enterprise"
[5]: https://www.dataiku.com/product "Dataiku Product"
[6]: https://learn.microsoft.com/en-us/power-bi/create-reports/copilot-semantic-models "Microsoft Learn — Copilot with semantic models"
